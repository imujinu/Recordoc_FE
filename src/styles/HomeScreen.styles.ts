import { StyleSheet } from 'react-native';
import { Colors } from './theme';

export const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: Colors.bg },
  header: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingHorizontal: 20,
    paddingVertical: 12,
    paddingTop: 40,
    backgroundColor: Colors.white,
    borderBottomWidth: 0.5,
    borderBottomColor: Colors.border,
  },
  logo: { fontSize: 20, fontWeight: '700', color: Colors.mint },
  avatarBtn: {
    width: 36,
    height: 36,
    borderRadius: 18,
    backgroundColor: Colors.mintLight,
    justifyContent: 'center',
    alignItems: 'center',
  },
  scroll: { padding: 18, paddingBottom: 32 },
  sectionTitle: {
    fontSize: 13,
    fontWeight: '600',
    color: '#888',
    marginBottom: 12,
    marginTop: 8,
    letterSpacing: 0.3,
  },
  grid: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginBottom: 24,
  },
  gridItem: {
    width: '23%',
    backgroundColor: Colors.white,
    borderRadius: 14,
    borderWidth: 0.5,
    borderColor: Colors.borderLight,
    alignItems: 'center',
    paddingVertical: 14,
    paddingHorizontal: 4,
  },
  gridIcon: {
    width: 44,
    height: 44,
    borderRadius: 12,
    backgroundColor: Colors.mintLight,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 8,
  },
  gridLabel: { fontSize: 11, color: Colors.textMid, fontWeight: '500', textAlign: 'center' },
  emptyBox: {
    backgroundColor: Colors.white,
    borderRadius: 14,
    borderWidth: 0.5,
    borderColor: Colors.borderLight,
    padding: 32,
    alignItems: 'center',
  },
  emptyText: { fontSize: 14, color: Colors.textLight, textAlign: 'center', lineHeight: 22 },
  workItem: {
    backgroundColor: Colors.white,
    borderRadius: 14,
    borderWidth: 0.5,
    borderColor: Colors.borderLight,
    padding: 14,
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
    marginBottom: 8,
  },
  workIcon: {
    width: 44,
    height: 44,
    borderRadius: 12,
    backgroundColor: Colors.mintLight,
    justifyContent: 'center',
    alignItems: 'center',
  },
  workType: { fontSize: 11, color: Colors.mint, fontWeight: '600' },
  workTitle: { fontSize: 13, color: Colors.textDark, fontWeight: '500', marginTop: 2 },
});
